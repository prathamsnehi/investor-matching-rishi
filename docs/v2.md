## Plans for v2:

1. Fine tuned cross encoder as the third stage of the app. 
2. Switch BM25 sparse embeddings with a base/fine tuned SPLADE sparse embedding model.
3. Add support for datatypes other than PDFs and text like images, pptx etc. Add a data preprocessing pipeline using CV